/**
 * @swagger
 * /Student/:id:
 *   get:
 *     description:  test endpoint
 *     tags:
 *       - Student
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */

/**
 * @swagger
 * /Student:
 *   post:
 *     description:  test endpoint
 *     tags:
 *       - Student
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             type: object
 *             properties:
 *               title:
 *                 type: string
 *                 description: The Student's title.
 *                 example: Leanne
 *               type:
 *                 type: string
 *                 description: The Student's type, value between vacation and expert.
 *                 example: "vacation"
 * 
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */
/**
 * @swagger
 * /Student:
 *   put:
 *     description:  test endpoint
 *     tags:
 *       - Student
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             type: object
 *             properties:
 *               title:
 *                 type: string
 *                 description: The Student's title.
 *                 example: Leanne
 *               type:
 *                 type: string
 *                 description: The Student's type, value between vacation and expert.
 *                 example: "vacation"
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */

/**
 * @swagger
 * /Student/:id:
 *   delete:
 *     description:  test endpoint
 *     tags:
 *       - Student
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */
/**
 * @swagger
 * /Internship/:id:
 *   get:
 *     description:  test endpoint
 *     tags:
 *       - Internship
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */

/**
 * @swagger
 * /Internship:
 *   post:
 *     description:  test endpoint
 *     tags:
 *       - Internship
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             type: object
 *             properties:
 *               title:
 *                 type: string
 *                 description: The Internship's title.
 *                 example: Leanne
 *               type:
 *                 type: string
 *                 description: The Internship's type, value between vacation and expert.
 *                 example: "vacation"
 * 
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */
/**
 * @swagger
 * /Internship:
 *   put:
 *     description:  test endpoint
 *     tags:
 *       - Internship
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             type: object
 *             properties:
 *               title:
 *                 type: string
 *                 description: The Internship's title.
 *                 example: Leanne
 *               type:
 *                 type: string
 *                 description: The Internship's type, value between vacation and expert.
 *                 example: "vacation"
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */

/**
 * @swagger
 * /Internship/:id:
 *   delete:
 *     description:  test endpoint
 *     tags:
 *       - Internship
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */
/**
 * @swagger
 * /Resume/:id:
 *   get:
 *     description:  test endpoint
 *     tags:
 *       - Resume
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */

/**
 * @swagger
 * /Resume:
 *   post:
 *     description:  test endpoint
 *     tags:
 *       - Resume
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             type: object
 *             properties:
 *               title:
 *                 type: string
 *                 description: The Resume's title.
 *                 example: Leanne
 *               type:
 *                 type: string
 *                 description: The Resume's type, value between vacation and expert.
 *                 example: "vacation"
 * 
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */
/**
 * @swagger
 * /Resume:
 *   put:
 *     description:  test endpoint
 *     tags:
 *       - Resume
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             type: object
 *             properties:
 *               title:
 *                 type: string
 *                 description: The Resume's title.
 *                 example: Leanne
 *               type:
 *                 type: string
 *                 description: The Resume's type, value between vacation and expert.
 *                 example: "vacation"
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */

/**
 * @swagger
 * /Resume/:id:
 *   delete:
 *     description:  test endpoint
 *     tags:
 *       - Resume
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */
/**
 * @swagger
 * /Demande/:id:
 *   get:
 *     description:  test endpoint
 *     tags:
 *       - Demande
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */

/**
 * @swagger
 * /Demande:
 *   post:
 *     description:  test endpoint
 *     tags:
 *       - Demande
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             type: object
 *             properties:
 *               title:
 *                 type: string
 *                 description: The Demande's title.
 *                 example: Leanne
 *               type:
 *                 type: string
 *                 description: The Demande's type, value between vacation and expert.
 *                 example: "vacation"
 * 
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */
/**
 * @swagger
 * /Demande:
 *   put:
 *     description:  test endpoint
 *     tags:
 *       - Demande
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             type: object
 *             properties:
 *               title:
 *                 type: string
 *                 description: The Demande's title.
 *                 example: Leanne
 *               type:
 *                 type: string
 *                 description: The Demande's type, value between vacation and expert.
 *                 example: "vacation"
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */

/**
 * @swagger
 * /Demande/:id:
 *   delete:
 *     description:  test endpoint
 *     tags:
 *       - Demande
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */
/**
 * @swagger
 * /Event/:id:
 *   get:
 *     description:  test endpoint
 *     tags:
 *       - Event
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */

/**
 * @swagger
 * /Event:
 *   post:
 *     description:  test endpoint
 *     tags:
 *       - Event
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             type: object
 *             properties:
 *               title:
 *                 type: string
 *                 description: The Event's title.
 *                 example: Leanne
 *               type:
 *                 type: string
 *                 description: The Event's type, value between vacation and expert.
 *                 example: "vacation"
 * 
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */
/**
 * @swagger
 * /Event:
 *   put:
 *     description:  test endpoint
 *     tags:
 *       - Event
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             type: object
 *             properties:
 *               title:
 *                 type: string
 *                 description: The Event's title.
 *                 example: Leanne
 *               type:
 *                 type: string
 *                 description: The Event's type, value between vacation and expert.
 *                 example: "vacation"
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */

/**
 * @swagger
 * /Event/:id:
 *   delete:
 *     description:  test endpoint
 *     tags:
 *       - Event
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */
/**
 * @swagger
 * /Offer/:id:
 *   get:
 *     description:  test endpoint
 *     tags:
 *       - Offer
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */

/**
 * @swagger
 * /Offer:
 *   post:
 *     description:  test endpoint
 *     tags:
 *       - Offer
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             type: object
 *             properties:
 *               title:
 *                 type: string
 *                 description: The Offer's title.
 *                 example: Leanne
 *               type:
 *                 type: string
 *                 description: The Offer's type, value between vacation and expert.
 *                 example: "vacation"
 * 
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */
/**
 * @swagger
 * /Offer:
 *   put:
 *     description:  test endpoint
 *     tags:
 *       - Offer
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             type: object
 *             properties:
 *               title:
 *                 type: string
 *                 description: The Offer's title.
 *                 example: Leanne
 *               type:
 *                 type: string
 *                 description: The Offer's type, value between vacation and expert.
 *                 example: "vacation"
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */

/**
 * @swagger
 * /Offer/:id:
 *   delete:
 *     description:  test endpoint
 *     tags:
 *       - Offer
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */
/**
 * @swagger
 * /Personnel/:id:
 *   get:
 *     description:  test endpoint
 *     tags:
 *       - Personnel
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */

/**
 * @swagger
 * /Personnel:
 *   post:
 *     description:  test endpoint
 *     tags:
 *       - Personnel
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             type: object
 *             properties:
 *               title:
 *                 type: string
 *                 description: The Personnel's title.
 *                 example: Leanne
 *               type:
 *                 type: string
 *                 description: The Personnel's type, value between vacation and expert.
 *                 example: "vacation"
 * 
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */
/**
 * @swagger
 * /Personnel:
 *   put:
 *     description:  test endpoint
 *     tags:
 *       - Personnel
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             type: object
 *             properties:
 *               title:
 *                 type: string
 *                 description: The Personnel's title.
 *                 example: Leanne
 *               type:
 *                 type: string
 *                 description: The Personnel's type, value between vacation and expert.
 *                 example: "vacation"
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */

/**
 * @swagger
 * /Personnel/:id:
 *   delete:
 *     description:  test endpoint
 *     tags:
 *       - Personnel
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */
/**
 * @swagger
 * /Pfa/:id:
 *   get:
 *     description:  test endpoint
 *     tags:
 *       - Pfa
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */

/**
 * @swagger
 * /Pfa:
 *   post:
 *     description:  test endpoint
 *     tags:
 *       - Pfa
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             type: object
 *             properties:
 *               title:
 *                 type: string
 *                 description: The Pfa's title.
 *                 example: Leanne
 *               type:
 *                 type: string
 *                 description: The Pfa's type, value between vacation and expert.
 *                 example: "vacation"
 * 
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */
/**
 * @swagger
 * /Pfa:
 *   put:
 *     description:  test endpoint
 *     tags:
 *       - Pfa
 *     requestBody:
 *       required: true
 *       content:
 *         application/json:
 *           schema:
 *             type: object
 *             properties:
 *               title:
 *                 type: string
 *                 description: The Pfa's title.
 *                 example: Leanne
 *               type:
 *                 type: string
 *                 description: The Pfa's type, value between vacation and expert.
 *                 example: "vacation"
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */

/**
 * @swagger
 * /Pfa/:id:
 *   delete:
 *     description:  test endpoint
 *     tags:
 *       - Pfa
 *     responses:
 *       200:
 *         description: Successfully login
 *         schema:
 *           properties:
 *             status:
 *               type: string
 *             faId:
 *               type: string
 */
